# Data

The scripts download public benchmarks through HuggingFace `datasets` or the relevant evaluation package. Raw datasets and model checkpoints are not redistributed in this supplement.

Use `HF_HOME` to control the local cache location:

```bash
HF_HOME=/path/to/hf-cache bash scripts/quick_start.sh
```
