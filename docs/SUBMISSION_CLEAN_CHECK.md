# Submission clean check

Before uploading the supplement, run a recursive search for personal names, emails, institutional identifiers, local machine paths, and account-specific identifiers.

Expected result: no personal paths, names, emails, institution names, API keys, or account-specific identifiers should appear in the uploaded archive. Dataset and model identifiers are retained only when needed to reproduce the experiments.
