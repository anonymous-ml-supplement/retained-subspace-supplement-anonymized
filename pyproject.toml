[project]
name = "retained-subspace-selection-supplement"
version = "0.1.0"
description = "Supplementary experiment code for retained-subspace selection in trained LoRA adapters."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
